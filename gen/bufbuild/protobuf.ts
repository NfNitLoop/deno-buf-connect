// while bufbuild *does* support a rewrite, it doesn't let us specify a URL.
// It does support relative imports, though, so this file is that relative import.

// export * from "https://cdn.skypack.dev/@bufbuild/protobuf@v1.2.0?dts"

export * from "https://esm.sh/@bufbuild/protobuf@v1.2.0"